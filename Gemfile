source "https://rubygems.org"

# GitHub Pages gem pins Jekyll and all whitelisted plugins to the exact
# versions GitHub Pages runs in production, so local preview matches deploys.
gem "github-pages", group: :jekyll_plugins

# Required for `jekyll serve` on Ruby 3.x (webrick is no longer bundled).
gem "webrick", "~> 1.8"
